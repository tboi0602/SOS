{
  "name": "tamtriluc",
  "version": "1.0.0",
  "description": "sửa lại .env để deploy\r bash scripts/deploy.sh .env.docker",
  "homepage": "https://github.com/tboi0602/SOS#readme",
  "bugs": {
    "url": "https://github.com/tboi0602/SOS/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tboi0602/SOS.git"
  },
  "scripts": {
    "deploy": "wsl sh scripts/deploy.sh .env.docker",
    "deploy:all": "wsl sh scripts/deploy.sh .env.docker",
    "deploy:vps-only": "wsl sh scripts/deploy.sh .env.docker --vps-only",
    "deploy:build-only": "wsl sh scripts/deploy.sh .env.docker --no-deploy"
  }
}
